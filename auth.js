const express=require("express"),router=express.Router(),authController=require("../controllers/authcont");router.post("/register",authController.register),router.post("/login",authController.login),router.post("/logout",((e,r)=>{e.session.destroy((e=>{if(e)return console.error("Erreur de session:",e),r.status(500).json({message:"Erreur lors de la déconnexion"});r.clearCookie("connect.sid"),r.status(200).json({message:"Déconnexion réussie"})}))})),module.exports=router;